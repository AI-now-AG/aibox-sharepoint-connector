---
const { theme } = Astro.locals.tenant;
---

<script define:vars={{ theme }} is:inline>
  // set default theme in storage
  const savedTheme = localStorage.getItem("theme");
  if (theme && !savedTheme) {
    document.documentElement.setAttribute("data-theme", theme);
  }
</script>
