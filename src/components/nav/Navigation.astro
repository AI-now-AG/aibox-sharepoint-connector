---
import MenuItem from "./MenuItem.astro";

const modules = [
  {
    title: "Text writing",
    items: [
      {
        title: "Headline",
        path: "/headlines",
      },
      {
        title: "Summarize",
        path: "/summarize",
      },
      {
        title: "Rewrite",
        path: "/rewrite",
      },
    ],
  },
];
---

<nav class="min-h-full block flex">
  <ul class="menu bg-base-300 min-h-full w-64">
    <li class="text-lg pl-4 py-4">Widgets</li>
    {
      modules.map((mod) => (
        <li>
          <MenuItem {...mod} />

          {"items" in mod && (
            <ul>
              {mod.items.map((sub) => (
                <li>
                  <MenuItem {...sub} />
                </li>
              ))}
            </ul>
          )}
        </li>
      ))
    }
  </ul>
</nav>
