---
import Layout from "$layouts/Layout.astro";
import { useTranslations } from "$i18n/utils";
import TenantFrom from "./TenantFrom.svelte";

const t = useTranslations();
---

<Layout>
  <TenantFrom client:load tenant={undefined} />
</Layout>
