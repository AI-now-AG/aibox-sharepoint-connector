import { c as create_ssr_component, e as escape, b as add_attribute, d as each, v as validate_component } from './Layout_i1A0xYMe.mjs';
import { u as useTranslations } from './Head_BGho-qey.mjs';

/* src/pages/prompt-library/prompts/SingleInput.svelte generated by Svelte v4.2.18 */

const SingleInput = create_ssr_component(($$result, $$props, $$bindings, slots) => {
	let { title } = $$props;
	let { placeholder } = $$props;
	let { items } = $$props;
	let { selectedItem } = $$props;
	let inputValue = "";

	const resetSelection = () => {
		selectedItem = void 0;
		setInputValue();
	};

	function setInputValue() {
		inputValue = selectedItem && (typeof selectedItem === "string"
		? selectedItem
		: selectedItem.title) || "";
	}

	if ($$props.title === void 0 && $$bindings.title && title !== void 0) $$bindings.title(title);
	if ($$props.placeholder === void 0 && $$bindings.placeholder && placeholder !== void 0) $$bindings.placeholder(placeholder);
	if ($$props.items === void 0 && $$bindings.items && items !== void 0) $$bindings.items(items);
	if ($$props.selectedItem === void 0 && $$bindings.selectedItem && selectedItem !== void 0) $$bindings.selectedItem(selectedItem);

	{
		{
			if (selectedItem) {
				setInputValue();
			} else if (items) {
				resetSelection();
			}
		}
	}

	return `<div><p class="mb-2">${escape(title)}</p> <div class="dropdown dropdown-bottom w-full min-w-xs"><input${add_attribute("placeholder", placeholder, 0)} tabindex="2" role="button" class="input input-bordered font-medium w-full min-w-xs" readonly${add_attribute("value", inputValue, 0)}> <ul tabindex="2" class="dropdown-content menu bg-base-100 space-y-2 rounded-box z-[1] w-52 p-2 shadow">${items
	? `${each(items, item => {
			return `<li><button${add_attribute(
				"class",
				`${selectedItem == item
				? "bg-primary text-base-100 hover:bg-primary"
				: "hover:text-neutral"}`,
				0
			)}>${escape(typeof item === "string" ? item : item.title)}</button> </li>`;
		})}`
	: ``}</ul></div></div>`;
});

/* src/pages/prompt-library/prompts/MultiInput.svelte generated by Svelte v4.2.18 */

const MultiInput = create_ssr_component(($$result, $$props, $$bindings, slots) => {

	let { title } = $$props;
	let { placeholder } = $$props;
	let { items } = $$props;
	let { selectedItems } = $$props;
	let inputValue = "";

	const resetSelection = () => {
		selectedItems = [];
		setInputValue();
	};

	function setInputValue() {
		inputValue = selectedItems?.map(e => e.title).join(", ");
	}

	if ($$props.title === void 0 && $$bindings.title && title !== void 0) $$bindings.title(title);
	if ($$props.placeholder === void 0 && $$bindings.placeholder && placeholder !== void 0) $$bindings.placeholder(placeholder);
	if ($$props.items === void 0 && $$bindings.items && items !== void 0) $$bindings.items(items);
	if ($$props.selectedItems === void 0 && $$bindings.selectedItems && selectedItems !== void 0) $$bindings.selectedItems(selectedItems);

	{
		{
			if (selectedItems.length > 0) {
				setInputValue();
			} else if (items) {
				resetSelection();
			}
		}
	}

	return `<div><p class="mb-2">${escape(title)}</p> <div class="dropdown dropdown-bottom w-full min-w-xs"><label class="input input-bordered flex items-center gap-2"><svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg" class="mr-2"><path d="M13 13L9 9M10.3333 5.66667C10.3333 8.244 8.244 10.3333 5.66667 10.3333C3.08934 10.3333 1 8.244 1 5.66667C1 3.08934 3.08934 1 5.66667 1C8.244 1 10.3333 3.08934 10.3333 5.66667Z" stroke="#111827" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg> <input type="text"${add_attribute("placeholder", placeholder, 0)} tabindex="9" role="button" class="grow font-medium w-full min-w-xs" readonly${add_attribute("value", inputValue, 0)}> <svg width="12" height="7" viewBox="0 0 12 7" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M10.6663 1L5.99967 5.66667L1.33301 1" stroke="#111827" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg></label> ${items
	? `<ul tabindex="9" class="dropdown-content menu bg-base-100 space-y-2 rounded-box z-[1] w-52 p-2 shadow">${each(items, item => {
			return `<li><a${add_attribute(
				"class",
				`${(selectedItems?.includes(item))
				? "bg-primary text-base-100 hover:bg-primary"
				: "hover:text-neutral"}`,
				0
			)}>${escape(item.title)}</a> </li>`;
		})}</ul>`
	: ``}</div></div>`;
});

/* src/pages/prompt-library/prompts/PromptForm.svelte generated by Svelte v4.2.18 */

const PromptForm = create_ssr_component(($$result, $$props, $$bindings, slots) => {
	let { preferredLocale } = $$props;
	const t = useTranslations(preferredLocale);
	let categories = [];
	let selectedCategory;
	let selectedGroup;
	let instructions = [];
	let selectedInstructions = [];
	let knowledgeBases = [];
	let selectedKnowledgeBases = [];
	let promptTitle = "";
	let { promptId } = $$props;
	let { prompt } = $$props;
	let isSaving = false;

	if ($$props.preferredLocale === void 0 && $$bindings.preferredLocale && preferredLocale !== void 0) $$bindings.preferredLocale(preferredLocale);
	if ($$props.promptId === void 0 && $$bindings.promptId && promptId !== void 0) $$bindings.promptId(promptId);
	if ($$props.prompt === void 0 && $$bindings.prompt && prompt !== void 0) $$bindings.prompt(prompt);
	let $$settled;
	let $$rendered;
	let previous_head = $$result.head;

	do {
		$$settled = true;

		// $$result.head is mutated by the literal expression
		// need to reset it if we're looping back to prevent duplication
		$$result.head = previous_head;

		$$rendered = `<div class="container max-w-5xl p-6 mx-auto p-4"><div class="w-full min-w-xs pt-2 lg:pt-6"><h1 class="pt-2 text-4xl font-bold pb-6">${prompt
		? `${escape(t("prompt-library.prompts.edit"))}`
		: `${escape(t("prompt-library.prompts.add"))}`}</h1> <form class="rounded pt-6 mb-4 space-y-6"><div class="grid grid-cols-1 gap-4 justify-center"><div><p class="mb-2">${escape(t("prompt-library.add.prompts.title"))}</p> <input type="text" placeholder="e.g. Create three sports headlines" class="input input-bordered w-full min-w-xs"${add_attribute("value", promptTitle, 0)}></div></div> <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-2 gap-4 justify-center">${validate_component(SingleInput, "SingleInput").$$render(
			$$result,
			{
				title: t("prompt-library.add.prompts.category"),
				placeholder: "e.g. Editing",
				items: categories,
				selectedItem: selectedCategory
			},
			{
				selectedItem: $$value => {
					selectedCategory = $$value;
					$$settled = false;
				}
			},
			{}
		)} ${selectedCategory
		? `${validate_component(SingleInput, "SingleInput").$$render(
				$$result,
				{
					title: t("prompt-library.add.prompts.group"),
					placeholder: "e.g. Headlines",
					items: selectedCategory.groups,
					selectedItem: selectedGroup
				},
				{
					selectedItem: $$value => {
						selectedGroup = $$value;
						$$settled = false;
					}
				},
				{}
			)}`
		: ``}</div> <div class="mb-4"><p class="mb-2">${escape(t("prompt-library.add.prompts.prompt"))}</p> <textarea placeholder="e.g. Create three headlines..." class="input input-bordered min-w-xs shadow appearance-none min-h-32 w-full py-2 px-3">${escape("")}</textarea></div> <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-2 gap-4 justify-center">${validate_component(MultiInput, "MultiInput").$$render(
			$$result,
			{
				title: t("prompt-library.add.prompts.instructions"),
				placeholder: "e.g. Instruction",
				items: instructions,
				selectedItems: selectedInstructions
			},
			{
				selectedItems: $$value => {
					selectedInstructions = $$value;
					$$settled = false;
				}
			},
			{}
		)} ${validate_component(MultiInput, "MultiInput").$$render(
			$$result,
			{
				title: t("prompt-library.add.prompts.knowledge-base"),
				placeholder: "e.g. Knowledge base",
				items: knowledgeBases,
				selectedItems: selectedKnowledgeBases
			},
			{
				selectedItems: $$value => {
					selectedKnowledgeBases = $$value;
					$$settled = false;
				}
			},
			{}
		)} </div> <div class="flex items-center justify-between"><button${add_attribute("class", `btn btn-active btn-primary px-8 font-normal ${isSaving}`, 0)}>${`${escape(t("prompt-library.add.prompts.save"))}`}</button> <button class="btn btn-active btn-ghost px-8 font-normal" data-svelte-h="svelte-wpc8yo">Try It Out</button></div></form></div></div>`;
	} while (!$$settled);

	return $$rendered;
});

export { PromptForm as P };
